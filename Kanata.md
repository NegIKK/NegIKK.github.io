---
pin: true
date: 2026-01-14 19:55
tags:
  - софт/утилиты
share: true
---
## Официальный гайд
```cardlink
url: https://jtroo.github.io/config.html
title: "Kanata Configuration Guide"
host: jtroo.github.io
```

[Пример](https://github.com/jtroo/kanata/blob/main/cfg_samples/tray-icon/tray-icon.kbd) настроек для использования иконки в трее. Я так понял лучше использовать версию которая запускается как приложение в трее, чтоб не втсела консоль в фоне

В целом и без любых настроек уже работает. Это скорее допки и полишинг

- - -

[Статья](https://shom.dev/start/using-kanata-to-remap-any-keyboard/) гайд какого-то чела вроде подробно

---

## Видео

Попытка сделать команды по видео ниже но не вышло, мб на будущее пригодится еще

просто запускается старт командой ниже.

```python
[Environment]::SetEnvironmentVariable( `
	"Path", `
	[Environment]::GetEnvironmentVariable( `
		"Path", `
		[System.EnvironmentVariableTarget]::User `
	) + ";" + (Get-Location), `
	[System.EnvironmentVariableTarget]::User `
)
```

```python
[Environment]::SetEnvironmentVariable( ` "Path", ` [Environment]::GetEnvironmentVariable( ` "Path", ` [System.EnvironmentVariableTarget]::User ` ) + ";" + (Get-Location), ` [System.EnvironmentVariableTarget]::User ` )
```

```cardlink
url: https://www.youtube.com/watch?v=wVp9zEXqjSs
title: "Изменение поведения ЛЮБОЙ клавиатуры. Введение в Kanata"
description: "Рассказываю как можно кастомизировать поведение любой клавиатуры (даже не программируемой) с помощью Kanata. 🖈 Полезные ссылки 🖈Итоговый проект: https://gi..."
host: www.youtube.com
favicon: https://www.youtube.com/s/desktop/5d0caf43/img/favicon_32x32.png
image: https://i.ytimg.com/vi/wVp9zEXqjSs/maxresdefault.jpg
```


---

## Мои заметки

Старт канаты (Мой)

Переходим в директорию с экзешником и вызываем его с настройками в конфиге

```python
kanata.exe --cfg config.kbd
```

## Сокращения для модификаторов

![[./Заметки/files/Pasted image 20260110133347.png|Pasted image 20260110133347.png]]

## Ввод комбинаций клавиш (Аккорды)

[версия 2](https://jtroo.github.io/config.html#input-chords-v2)

[версия 1](https://jtroo.github.io/config.html#input-chords)

Пример написания

```python
(defchordsv2
  (participating-keys1) action1 timeout1 release-behaviour1 (disabled-layers1)
    ...
  (participating-keysN) actionN timeoutN release-behaviourN (disabled-layersN)
)
```

- `$participating-keys` — Клавиши, которые мы хотим назначить на аккорд. Они должны быть в `defsrc`
- `$action` — Действие, которые настраиваются в `deflayer` или `defalias`
- `$timeout` — время в миллисекундах, внутри которого нажатие на эти клавиши будет считаться аккордом
- `$release-behaviour` — This must be either `first-release` or `all-released`; `first-release` means the chord action will be released when the first participant is released, while `all-released` means the chord action will be released only when all of the participants have been released.
- `$disabled-layers` — A list of layer names on which this chord is disabled.

---

## Область для тестов

раз два три выть не казаться привет вывывыывывыв привет бял вызовыгрыва

jfjsdflskdjf;a

dfssdf fsdffsdffddfsfDffsdsf

привет жопа ываааываоаывлаоддлоааавваыва

проверка тест нового подхода вроде стало лучше

и в то же проверка один два три Довольно интересно конечно, но все равно лагает Хотя все Равно Лучше и Немного Проще писать Большие буквы

то же самое но с меньшей задержкой Ну конечно лучше, но все равно как-то конечно не

привет как дела у вас чуваки

привет чуваки как дела у вас

С большой буквы Рисовать всякую Хуйню это конечно весело

ывалоддлывоадыоадлывоадлдлоджылылЛицо жопа и вообще прикольно конечно кстати как дела ебать Мужики, суки Рыло Жопа хуй и Залупа Сука Бля ёёёё